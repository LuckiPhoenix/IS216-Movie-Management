{
    "name": "my-app",
    "private": true,
    "scripts": {
        "frontend": "npm run dev --prefix apps/website",
        "backend": "cd apps/server && ./gradlew bootRun --args='--spring.profiles.active=dev'",
        "dev": "concurrently \"npm run frontend\" \"npm run backend\""
    },
    "devDependencies": {
      "concurrently": "^8.0.0"
    }
  }