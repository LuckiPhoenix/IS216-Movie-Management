{
  "name": "my-app",
  "private": true,
  "scripts": {
    "frontend": "npm run dev --prefix apps/website",
    "backend": "cd apps/server && ./gradlew bootRun --args='--spring.profiles.active=dev'",
    "dev": "concurrently \"npm run frontend\" \"npm run backend\"",
    "prepare": "husky"
  },
  "devDependencies": {
    "concurrently": "^8.0.0",
    "husky": "^9.1.7"
  },
  "lint-staged": {
  "apps/website/**/*.{js,ts,jsx,tsx}": [
    "cd apps/website && npx eslint --fix",
    "cd apps/website && npx prettier --write"
  ]
}
}
